1. Basic commands of git
git init

git clone url

git add filename or .

git commit -m "message"

git status

git push

git pull

git log

2. For first time git initalization in the folder and after that adding to github.
git init

create README.md in local directory.

git status

git add .

git commit -m "message"

git remote add origin <URL github>

git checkout -b main

git push origin main, if other branchs - git push -u origin branchname


3. For pulling changes from github
git pull origin main


4. All about branching
git branch - List all branches

git branch [name] - Create new branch

git checkout [name] - Switch to branch

git checkout -b [name] - Create & switch branch

git merge [branch] - Merge branch into current

git branch -d [name] - Delete branch


5. 
